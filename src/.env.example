# Environment example for TaskFlow
# Copy this to .env and set real values in your deployment (do NOT commit .env)

# App environment: development|production
APP_ENV=development
APP_URL=http://localhost/php

# Base URL (relative)
BASE_URL=/php

# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=taskflow2
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# Mail Configuration
# Driver: mail, smtp, log (use 'log' for development)
MAIL_DRIVER=log
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@taskflow.local
MAIL_FROM_NAME=TaskFlow

# Other secrets
# APP_KEY=generate_a_random_key_here
